---
title: Reset Password
description: Alur reset password untuk Next.js 16 dengan Better Auth. Konfigurasi token reset password, callback email, dan server actions untuk lupa/reset password.
---

{/*
  Halaman ini dalam Bahasa Indonesia.
  Untuk menerjemahkan halaman lain:
  1. Copy file dari content/docs/en/ ke content/docs/id/ dengan nama yang sama
  2. Terjemahkan frontmatter (title, description)
  3. Terjemahkan heading dan teks prose
  4. JANGAN terjemahkan: code blocks, URL, nama fitur/API, nama variabel, command CLI
  5. Pastikan semua link internal tetap berfungsi (tetap gunakan path yang sama)
*/}

# Reset Password

Alur reset password menggunakan Better Auth. Dua langkah: minta link reset, lalu atur password baru.

## Konfigurasi

### Mengaktifkan reset password

```typescript
import { defineAuthConfig } from "@/auth/config";

export default defineAuthConfig({
  features: {
    emailPassword: {
      enabled: true,
    },
    passwordReset: true, // atau { expiresIn: "2h" }
  },
});
```

Standarnya `false`. Atur ke `true` untuk mengaktifkan dengan masa berlaku token standar, atau berikan `{ expiresIn: "1h" }` untuk kustomisasi.

### Menyediakan callback pengiriman email

```typescript
export default defineAuthConfig({
  email: {
    sendPasswordResetEmail: async ({ email, url, token }) => {
      // Kirim email menggunakan penyedia pilihan Anda
      console.log(`Password reset URL for ${email}: ${url}`);
    },
  },
  features: {
    emailPassword: { enabled: true },
    passwordReset: true,
  },
});
```

Jika tidak ada callback `sendPasswordResetEmail` yang disediakan, starter kit akan mencatat peringatan di mode development dan error di mode production.

### Masa berlaku token

Konfigurasikan melalui `passwordReset.expiresIn` (standar menggunakan bawaan Better Auth):

```typescript
passwordReset: {
  expiresIn: "1h", // "30m", "2h", "1d"
}
```

Opsi format durasi:

| Format | Contoh | Nilai |
|--------|--------|-------|
| `"30m"` | 30 menit | Token berumur pendek untuk keamanan |
| `"1h"` | 1 jam | Rekomendasi standar |
| `"2h"` | 2 jam | Waktu lebih panjang untuk pengiriman email lambat |
| `"1d"` | 24 jam | Tidak disarankan untuk keamanan |

## Server Actions

### `requestPasswordResetAction`

Form action untuk meminta email reset password.

```typescript
import { requestPasswordResetAction } from "@/lib/auth/actions/password-reset";

<form action={requestPasswordResetAction}>
  <input name="email" type="email" required />
  <button type="submit">Send reset link</button>
</form>
```

### `resetPasswordAction`

Form action yang memproses token reset dan password baru.

```typescript
import { resetPasswordAction } from "@/lib/auth/actions/password-reset";

<form action={resetPasswordAction}>
  <input name="token" type="hidden" value={token} />
  <input name="password" type="password" required />
  <button type="submit">Reset password</button>
</form>
```

## Halaman

| Halaman | Rute | Deskripsi |
|---------|------|-----------|
| Lupa Password | `/forgot-password` | Form untuk meminta link reset password |
| Reset Password | `/reset-password` | Form untuk mengatur password baru menggunakan token |

## Hooks

### `useForgotPasswordForm`

```tsx
import { useForgotPasswordForm } from "@/hooks/use-forgot-password";

function ForgotPasswordPage() {
  const [state, formAction, pending] = useForgotPasswordForm();
}
```

### `useResetPasswordForm`

```tsx
import { useResetPasswordForm } from "@/hooks/use-reset-password";

function ResetPasswordPage() {
  const [state, formAction, pending] = useResetPasswordForm();
}
```

## Terkait

- [Email & Password](./email-password) — Registrasi dan login
- [Verifikasi Email](./email-verification) — Alur verifikasi
- [Environment Variables](../get-started/environment-variables) — Pengaturan penyedia email
