# Review Fixes — 2026-06-25

## Decision: Search debounce

- **Issue**: Search fired API call on every keystroke with no debounce
- **Fix**: Replaced `useCallback` + direct fetch with `useEffect` + `setTimeout` (300ms) + cleanup function
- **File**: `components/search-dialog.tsx`

## Decision: Config builder UI completeness

- **Issue**: Builder output omitted `twoFactorSettingsPath`, `protectedPaths`, `roleRestrictions` — fields documented in the API reference
- **Fix**: Added all missing `ui` fields to `generateAuthConfig()`
- **File**: `components/config-builder.tsx`

## Deferred: Middleware deprecation

- Next.js 16 deprecates `middleware.ts` in favor of `proxy.ts`
- next-intl v4.13.0 doesn't export a `createProxy()` equivalent yet
- Will fix when next-intl ships proxy support

## Deferred: `public/sitemap.xml`

- Empty build artifact, harmless
- Real sitemap is generated by `next-sitemap` at build time
