# Docs Site Review — 2026-06-25

## Build
- **Status**: PASS — 0 errors, 0 TypeScript errors, 85 pages, sitemap generated
- **Warning**: middleware → proxy deprecation (blocked on next-intl)

## Tests (parent project)
- **166/166 passed** — all test files green

## Issues Found & Fixed

| Severity | Issue | Fix |
|----------|-------|-----|
| MEDIUM | Search fires API call on every keystroke | Added 300ms debounce in `search-dialog.tsx` |
| MEDIUM | Config builder missing `ui` fields documented in API reference | Added `twoFactorSettingsPath`, `protectedPaths`, `roleRestrictions` in `config-builder.tsx` |
| LOW | Middleware deprecation warning | Deferred until next-intl ships proxy support |
| LOW | `public/sitemap.xml` empty placeholder | Harmless — real sitemap generated by postbuild |

## What's Verified
- All 33 MDX pages + 8 section indexes present
- Frontmatter (title + description) on every page
- Heading hierarchy correct (h1 → h2 → h3, no skips)
- All internal links resolve
- `metadataBase`, `robots.txt`, `favicon.svg`, sitemap configured
- Search API works (dynamic route, excluded from middleware)
- i18n: en/id routes, messages, locale-aware sidebar
- Config builder generates complete output matching schema
